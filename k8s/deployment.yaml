apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-app
  namespace: sri-aplicacion
spec:
  replicas: 2
  selector:
    matchLabels:
      app: python-app
  template:
    metadata:
      labels:
        app: python-app
    spec:
      containers:
      - name: python-container
        # IMPORTANTE: Reemplaza con tu ID de cuenta de AWS y tu región - Es el ECR donde se encuentra tu imagen
        image: 509333930103.dkr.ecr.us-east-1.amazonaws.com/sri-app-py:9bbd28450464044f70e48158e1f8743bbf30ef50-1.0.0
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: python-app-service
  namespace: sri-aplicacion
spec:
  type: LoadBalancer # Esto creará un balanceador de carga en AWS con IP pública
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: python-app